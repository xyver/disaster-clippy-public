# Cloud deployment requirements (Railway - slim, no local ML)
#
# Railway serves two functions:
# 1. Public chat interface (Pinecone search + cloud LLMs)
# 2. Submissions gateway for local admins (R2 write + rate limiting)
#
# NOT needed: chromadb, sentence-transformers, transformers, zimply, scikit-learn
# Set VECTOR_DB_MODE=pinecone to disable admin UI

# Web framework
fastapi>=0.104.0
uvicorn>=0.24.0
jinja2>=3.1.0

# LLM and embeddings (API-based only)
langchain>=0.1.0
langchain-openai>=0.0.5
openai>=1.0.0
langchain-anthropic>=0.1.0

# Vector database (Pinecone cloud)
pinecone>=8.0.0

# HTTP clients
requests>=2.31.0
httpx>=0.25.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
numpy>=1.24.0

# Rate limiting (for submissions gateway)
slowapi>=0.1.9

# Cloud storage (R2 submissions bucket)
boto3>=1.34.0
